{
  "name": "social-rag-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "cd backend && .\\venv\\Scripts\\python.exe -m uvicorn main:app --reload --port 8000 --host 0.0.0.0",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "npm-run-all --parallel dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
